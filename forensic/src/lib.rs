//! `peripheral-forensic` — graded anomaly auditor over external-device
//! connections.
//!
//! Scaffold only; the `DeviceAnomaly` observations and `audit` land in the
//! following commits (TDD: tests first).

#![forbid(unsafe_code)]
