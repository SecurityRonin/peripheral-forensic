//! `peripheral-core` — external-device (peripheral) connection forensic reader.
//!
//! Scaffold only; the `DeviceConnection` model, `Bus` classifier, and the
//! `setupapi.dev.log` parser land in the following commits (TDD: tests first).

#![forbid(unsafe_code)]
